{
  "name": "codex-platform",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "web",
    "server"
  ],
  "scripts": {
    "dev:web": "npm run dev --prefix web",
    "dev:server": "npm run dev --prefix server"
  }
}
